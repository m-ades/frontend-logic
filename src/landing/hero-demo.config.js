/**
 * Hero “See HuLA in Action” demo — YouTube (optional).
 *
 * Paste either:
 *   - the 11-character video ID, or
 *   - a full watch / youtu.be / shorts / embed URL
 *
 * Leave empty (`''`) to keep the static poster image until your video is ready.
 */
export const HERO_DEMO_YOUTUBE_URL_OR_ID = 'https://youtu.be/rHJU_BtMd_E'

/**
 * Toggle the hero Mac + embed block and the “Video Demo” nav link.
 * Set to `true` when the recording is ready to show; headline + CTAs stay visible either way.
 */
export const HERO_DEMO_VIDEO_SECTION_ENABLED = true
